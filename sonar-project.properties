sonar.projectKey=jlrssrlj_blog
sonar.organization=jlrssrlj


# This is the name and version displayed in the SonarCloud UI.
#sonar.projectName=blog
#sonar.projectVersion=1.0

# --- optional properties ---

# defaults to project key
#sonar.projectName=My project
# defaults to 'not provided'
#sonar.projectVersion=1.0
 
# Path is relative to the sonar-project.properties file. Defaults to .
#sonar.sources=.
 
# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8
# Nombre del proyecto
sonar.projectKey=blog
sonar.projectName=blog
sonar.projectVersion=1.0

# Directorio de fuentes (excluye la máquina virtual)
sonar.sources=.

# Excluir la carpeta venv del análisis
sonar.exclusions=venv/**, .venv/**, **/migrations/**, **/__pycache__/**
sonar.exclusions=static/**, .static**

# Excluir archivos de pruebas del análisis
sonar.test.exclusions=**/tests/**

# Excluir la venv del análisis de duplicados
sonar.cpd.exclusions=venv/**, .venv/**

